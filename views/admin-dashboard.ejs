<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Dashboard</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  
  <style>
    body {
      background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
      min-height: 100vh;
      font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
    }

    .container {
      padding: 2rem 1rem;
      max-width: 1400px;
    }

    .welcome-banner {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      border-radius: 20px;
      padding: 2rem;
      color: white;
      margin-bottom: 2rem;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
      position: relative;
      overflow: hidden;
    }

    .welcome-banner::before {
      content: '';
      position: absolute;
      top: 0;
      right: 0;
      width: 300px;
      height: 100%;
      background: linear-gradient(45deg, transparent, rgba(255, 255, 255, 0.1));
      transform: skewX(-30deg);
    }

    .welcome-banner h1 {
      font-size: 2.5rem;
      font-weight: 600;
      margin-bottom: 0.5rem;
    }

    .welcome-banner p {
      font-size: 1.1rem;
      opacity: 0.9;
      margin: 0;
    }

    .dashboard-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 1.5rem;
      margin-bottom: 2rem;
    }

    .dashboard-card {
      background: rgba(255, 255, 255, 0.95);
      border-radius: 20px;
      padding: 1.5rem;
      transition: all 0.3s ease;
      position: relative;
      overflow: hidden;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    }

    .dashboard-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 15px 35px rgba(0, 0, 0, 0.15);
    }

    .dashboard-card .icon {
      font-size: 2.5rem;
      margin-bottom: 1rem;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .dashboard-card h3 {
      font-size: 1.5rem;
      font-weight: 600;
      color: #2d3748;
      margin-bottom: 0.5rem;
    }

    .dashboard-card p {
      color: #718096;
      margin-bottom: 1rem;
    }

    .dashboard-card .actions {
      display: flex;
      flex-wrap: wrap;
      gap: 0.5rem;
    }

    .action-btn {
      padding: 0.5rem 1rem;
      border-radius: 50px;
      font-size: 0.9rem;
      font-weight: 500;
      text-decoration: none;
      transition: all 0.3s ease;
      flex: 1;
      text-align: center;
      min-width: 120px;
    }

    .action-btn.primary {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
    }

    .action-btn.secondary {
      background: rgba(102, 126, 234, 0.1);
      color: #667eea;
    }

    .action-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(102, 126, 234, 0.2);
      color: white;
    }

    .action-btn.secondary:hover {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    }

    .quick-stats {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 1rem;
      margin-bottom: 2rem;
    }

    .stat-card {
      background: white;
      border-radius: 15px;
      padding: 1.5rem;
      text-align: center;
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
    }

    .stat-card .stat-icon {
      font-size: 2rem;
      margin-bottom: 0.5rem;
      color: #667eea;
    }

    .stat-card .stat-value {
      font-size: 1.8rem;
      font-weight: 600;
      color: #2d3748;
      margin-bottom: 0.25rem;
    }

    .stat-card .stat-label {
      color: #718096;
      font-size: 0.9rem;
    }

    @media (max-width: 768px) {
      .container {
        padding: 1rem;
      }

      .welcome-banner {
        padding: 1.5rem;
      }

      .welcome-banner h1 {
        font-size: 2rem;
      }

      .dashboard-grid {
        grid-template-columns: 1fr;
      }

      .quick-stats {
        grid-template-columns: repeat(2, 1fr);
      }
    }

    @media (max-width: 480px) {
      .quick-stats {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>
<body>
  <%- include('partials/navbar') %>
  <%- include('partials/admin-navbar') %>

  <div class="container">
    <div class="welcome-banner">
      <h1>Welcome Back, <%= user.name %>!</h1>
      <p>Access your administrative tools and manage your platform efficiently.</p>
    </div>

    <div class="quick-stats">
      <div class="stat-card">
        <i class="fas fa-users stat-icon"></i>
        <div class="stat-value"><%- typeof totalUsers !== 'undefined' ? totalUsers : '0' %></div>
        <div class="stat-label">Total Users</div>
      </div>
      <div class="stat-card">
        <i class="fas fa-exchange-alt stat-icon"></i>
        <div class="stat-value"><%- typeof totalTransactions !== 'undefined' ? totalTransactions : '0' %></div>
        <div class="stat-label">Transactions</div>
      </div>
      <div class="stat-card">
        <i class="fas fa-user-shield stat-icon"></i>
        <div class="stat-value"><%- typeof activeAdmins !== 'undefined' ? activeAdmins : '1' %></div>
        <div class="stat-label">Active Admins</div>
      </div>
      <div class="stat-card">
        <i class="fas fa-chart-line stat-icon"></i>
        <div class="stat-value"><%- typeof growthRate !== 'undefined' ? growthRate : '0' %>%</div>
        <div class="stat-label">Growth Rate</div>
      </div>
    </div>

    <div class="dashboard-grid">
      <div class="dashboard-card">
        <i class="fas fa-users-cog icon"></i>
        <h3>User Management</h3>
        <p>Manage user accounts, roles, and permissions with comprehensive controls.</p>
        <div class="actions">
          <a href="/admin/users" class="action-btn primary">View Users</a>
          <a href="/admin/add-user" class="action-btn secondary">Add User</a>
        </div>
      </div>

      <div class="dashboard-card">
        <i class="fas fa-chart-bar icon"></i>
        <h3>Analytics & Reports</h3>
        <p>Access detailed analytics and generate comprehensive reports.</p>
        <div class="actions">
          <a href="/admin/user-transactions" class="action-btn primary">Transactions</a>
          <a href="/admin/error-logs" class="action-btn secondary">Error Logs</a>
        </div>
      </div>

      <div class="dashboard-card">
        <i class="fas fa-cogs icon"></i>
        <h3>System Settings</h3>
        <p>Configure platform settings and customize user experience.</p>
        <div class="actions">
          <a href="/admin/settings" class="action-btn primary">Settings</a>
          <a href="/admin/settings/notifications" class="action-btn secondary">Notifications</a>
        </div>
      </div>

      <div class="dashboard-card">
        <i class="fas fa-user-shield icon"></i>
        <h3>Admin Controls</h3>
        <p>Access administrative tools and manage your profile.</p>
        <div class="actions">
          <a href="/profile" class="action-btn primary">Profile</a>
          <a href="/profile/edit" class="action-btn secondary">Edit Profile</a>
        </div>
      </div>

      <div class="dashboard-card">
        <i class="fas fa-shield-alt icon"></i>
        <h3>Security</h3>
        <p>Monitor and manage platform security settings.</p>
        <div class="actions">
          <a href="/admin/security" class="action-btn primary">Security Dashboard</a>
          <a href="/admin/security" class="action-btn secondary">Audit Logs</a>
        </div>
      </div>

      <div class="dashboard-card">
        <i class="fas fa-life-ring icon"></i>
        <h3>Support</h3>
        <p>Access support tools and manage user inquiries.</p>
        <div class="actions">
          <a href="/admin/support" class="action-btn primary">Support Center</a>
          <a href="/admin/tickets" class="action-btn secondary">View Tickets</a>
        </div>
      </div>
    </div>
  </div>

  <%- include('partials/footer') %>

  <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
</body>
</html>
